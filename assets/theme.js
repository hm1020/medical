import { Dimensions } from "react-native";
const { width, height } = Dimensions.get("window");

export const SIZES = {
  width,
  height,
  radius: 8,
};

export const COlORS = {
  primary: "#4361ee",
  opacityColor: "#bbd0f56f",
};
